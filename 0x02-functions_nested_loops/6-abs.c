#include "main.h"
/**
* _abs - prints the signs of an integer
* @n: is the integer whose value is evaluated
* Descriptio: return n for positive n 0 for zero
* and -n for negative n
* Return: -n for -ve 0 for zero and n for +ve
*/
int _abs(int n)
{
	if (n >= 0)
	return (n);

	else
	return (-n);
}
